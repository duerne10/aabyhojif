name: Update Topscorer

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  up*ate:
    runs-on: ubuntu-latest

 *  steps:
    - name: Checkout repo*itory
      uses: actions/checkout*v4

    - name: Download topscorer*JSON
      run: |
        curl -L*"https://duerne.sharepoint.com/:u:/s/AabyhojIF1Oldboys40aar/IQAVaY-SbkBWS7zrEgYU4szFAYw-k1tquCoRGGg9uEn63uM" -o data*topscorer.json

    - name: Commit*changes
      run: |
        git c*nfig user.name "GitHub Action"
   *    git config user.email "action@*ithub.com"

        git add*data/topscorer.json

        git*diff --staged --quiet || (
*         git*commit -m "Opdater topscorer"
    *     git push
        )
